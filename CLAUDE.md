# my_ai_web 워크스페이스

Claude Code 기반 바이브코딩 학습 워크스페이스입니다.

---

## 언어 설정

- 모든 답변은 **한국어**로 작성
- 정중하고 친근한 말투 사용
- 기술 용어는 한국어로 설명하되, 코드 내 식별자(변수명·함수명)는 영어 유지

---

## 워크스페이스 구조

```
my_ai_web/
├── CLAUDE.md              ← 현재 파일 (전역 설정)
├── .claude/
│   └── settings.local.json  ← 권한 및 타임아웃 설정
└── lecture1/              ← React + MUI 바이브코딩 강의
    ├── CLAUDE.md          ← 로키(Roki) 강의 어시스턴트 설정
    ├── docs/              ← 디자인 시스템 · 코드 컨벤션 · 프로젝트 가이드
    └── _template_settings/ ← MUI 완성 템플릿 (새 프로젝트 복사용)
```

---

## 개발 원칙

- 코드 품질과 가독성 우선
- 사용자 요청사항에 정확히 응답
- 큰 작업은 단계별로 나눠서 진행
- 각 단계 완료 시 결과 확인 후 다음 단계 진행

---

## 응답 방식

### 코드 작성 시
- 파일 경로와 함께 코드 제공 (`src/components/UserCard.jsx` 등)
- 기존 파일 수정이면 변경 부분만 명시
- 새 파일 생성이면 전체 코드 제공

### 설명 시
- 결론 먼저, 배경 나중
- 코드 예시를 통한 설명 선호
- 불필요한 장황한 설명 생략

### 에러 해결 시
1. 에러 원인 한 줄 요약
2. 수정 코드 제시
3. 필요 시 예방 방법 안내

---

## 주의사항

- `node_modules/` 직접 수정 금지
- 개발 서버(Vite) 종료 시 Claude Code 프로세스 건드리지 않기
- 패키지 설치는 항상 해당 프로젝트 디렉토리에서 실행
- 파일 삭제 전 반드시 확인 요청

---

## 강의 디렉토리별 설정

| 디렉토리 | CLAUDE.md | 설명 |
|----------|-----------|------|
| `lecture1/` | 로키(Roki) 역할 | React + MUI 바이브코딩 |

> 새 강의 디렉토리 추가 시 이 표에 업데이트
