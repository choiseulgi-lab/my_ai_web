# 컬러 팔레트 디자인 시스템

- **출처 웹사이트**: 바이오테크 SaaS 브랜드 (color.PNG 분석)
- **분석 날짜**: 2026-06-08
- **적용 프로젝트**: 포트폴리오 웹사이트

---

## CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary:            #C4E038;   /* 브랜드 라임 그린 — 로고, 아이콘, CTA */
  --color-primary-light:      #D6F04A;   /* 라임 그린 밝은 변형 — 호버, 차트 하이라이트 */
  --color-primary-dark:       #9DB82C;   /* 라임 그린 어두운 변형 — 눌림 상태, 강조 테두리 */

  /* Secondary Colors */
  --color-secondary:          #0E0E0E;   /* 니어 블랙 — 히어로 배경, 카드 배경 */
  --color-secondary-light:    #1E1E1E;   /* 니어 블랙 밝은 변형 — 카드 내부, 호버 배경 */
  --color-secondary-dark:     #000000;   /* 순수 블랙 — 그림자, 오버레이 */
  --color-accent:             #6BBF3C;   /* 차트 그린 — 데이터 시각화, 그라디언트 시작 */
  --color-accent-light:       #8FD458;   /* 차트 그린 밝은 변형 — 아이콘, 배지 */

  /* Background Colors */
  --color-bg-primary:         #0E0E0E;   /* 메인 다크 배경 */
  --color-bg-secondary:       #FFFFFF;   /* 콘텐츠 섹션 화이트 배경 */
  --color-bg-tertiary:        #F5F5F5;   /* 연회색 카드·컨테이너 배경 */
  --color-bg-brand:           #BCD830;   /* 브랜드 래퍼·그라디언트 배경 */

  /* Text Colors */
  --color-text-primary:       #FFFFFF;   /* 다크 배경 위 헤딩·본문 기본 텍스트 */
  --color-text-secondary:     #1A1A1A;   /* 라이트 배경 위 헤딩·본문 기본 텍스트 */
  --color-text-muted:         #888888;   /* 서브텍스트, 캡션, 날짜, 메타 정보 */
  --color-text-disabled:      #555555;   /* 비활성 상태 텍스트 */
  --color-text-accent:        #C4E038;   /* 강조 인라인 텍스트, 키워드 하이라이트 */
  --color-text-inverse:       #0E0E0E;   /* 라임 버튼 위 텍스트 (밝은 배경 위 어두운 글자) */

  /* Interactive Colors */
  --color-button-primary:     #C4E038;   /* Primary 버튼 배경 */
  --color-button-hover:       #D6F04A;   /* 버튼 호버 상태 */
  --color-link:               #C4E038;   /* 링크 기본 색상 */
  --color-link-hover:         #D6F04A;   /* 링크 호버 색상 */

  /* Border Colors */
  --color-border-dark:        #2A2A2A;   /* 다크 섹션 구분선 */
  --color-border-light:       #E0E0E0;   /* 라이트 섹션 구분선 */
}
```

---

## 컬러 사용 가이드

### Primary (`#C4E038` / `--color-primary`)
- 페이지에서 **가장 중요한 행동 유도 요소**에만 사용 (CTA 버튼, 핵심 아이콘, 브랜드 로고)
- 남용하면 시선 분산 — 전체 화면의 **15% 이하**로 유지
- 버튼에 적용 시 텍스트는 반드시 `--color-secondary(#0E0E0E)`로 설정 (명도 대비 확보)

### Secondary (`#0E0E0E` / `--color-secondary`)
- 히어로 섹션, 풀스크린 배경, 모달 오버레이 등 **무게감이 필요한 배경**에 사용
- 이 배경 위 텍스트는 `--color-text-primary(#FFFFFF)`만 사용

### Accent (`#6BBF3C` / `--color-accent`)
- 데이터 차트, 그래프, 진행 바 등 **시각화 요소 전용**
- `--color-primary-light(#D6F04A)`와 그라디언트 조합으로 사용 가능
  ```css
  background: linear-gradient(90deg, #6BBF3C, #D6F04A);
  ```

### Background
| 토큰 | 사용 시나리오 |
|------|-------------|
| `--color-bg-primary` | 히어로, 네비게이션, 풀페이지 다크 섹션 |
| `--color-bg-secondary` | 본문 콘텐츠, 텍스트 중심 섹션 |
| `--color-bg-tertiary` | 카드, 사이드바, 코드 블록 배경 |
| `--color-bg-brand` | 페이지 래퍼, 특별 프로모션 배너 |

### Text
| 토큰 | 사용 시나리오 |
|------|-------------|
| `--color-text-primary` | 다크 배경(`bg-primary`) 위 모든 텍스트 |
| `--color-text-secondary` | 라이트 배경(`bg-secondary`) 위 모든 텍스트 |
| `--color-text-muted` | 날짜, 태그, 부가 설명, placeholder |

### Interactive
- **버튼**: `background: var(--color-button-primary)` + `color: var(--color-secondary)`
- **호버 전환**: `transition: background 0.2s ease` 로 부드럽게 처리
- **링크**: 밑줄 제거 후 `--color-link` 색상으로 차별화, 호버 시 `--color-link-hover`

---

## 반응형 고려사항

### 다크모드 자동 대응

```css
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-secondary:    #1A1A1A;   /* 화이트 → 다크 그레이로 전환 */
    --color-bg-tertiary:     #2A2A2A;   /* 연회색 → 다크 카드 배경 */
    --color-text-secondary:  #F0F0F0;   /* 어두운 텍스트 → 밝은 텍스트 */
    --color-border-light:    #333333;   /* 라이트 보더 → 다크 보더 */
  }
}
```

### 모바일 (작은 화면) 고려사항
- `--color-primary(#C4E038)` 버튼은 터치 영역 최소 **44×44px** 확보
- 다크 배경 위 작은 텍스트(`< 14px`)는 `--color-text-muted` 대신 `#AAAAAA` 이상 밝기 유지 (WCAG AA 기준)

### 고대비 모드 대응

```css
@media (prefers-contrast: high) {
  :root {
    --color-primary:         #E8FF00;   /* 더 밝은 라임으로 대비 강화 */
    --color-text-muted:      #BBBBBB;   /* 뮤트 텍스트 밝기 상향 */
    --color-border-dark:     #555555;   /* 보더 더 뚜렷하게 */
  }
}
```

### 접근성 체크리스트

| 조합 | 대비 비율 | WCAG 기준 |
|------|-----------|-----------|
| `#FFFFFF` on `#0E0E0E` | 19.5 : 1 | AAA 통과 |
| `#0E0E0E` on `#C4E038` | 8.2 : 1 | AAA 통과 |
| `#1A1A1A` on `#FFFFFF` | 17.1 : 1 | AAA 통과 |
| `#888888` on `#FFFFFF` | 3.5 : 1 | AA 본문 주의 (18px+ 권장) |
